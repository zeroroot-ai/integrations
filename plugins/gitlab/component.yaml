apiVersion: component.gibson.zeroroot.ai/v1
kind: plugin
metadata:
  name: gitlab
  version: 0.1.0
  description: "GitLab plugin — curated read/write over the GitLab REST API."
spec:
  runtime: process
  manifest_path: ./plugin.yaml
